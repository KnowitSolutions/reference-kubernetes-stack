apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: keycloak
  namespace: login
spec:
  hosts:
    - sso.fake.no
  gateways:
    - keycloak
  http:
    - route:
        - destination:
            host: keycloak
